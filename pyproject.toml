[project]
name = "computer-use-automation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright",
    "pydantic>=2",
    "anthropic",
    "flask",
    "python-dotenv",
    "pytest",
    "mypy",
]
